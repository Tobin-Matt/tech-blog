<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The TeK Blog</title>
</head>

<body>
    <div>
        <header>
            <h1>
                <a href="/">The TeK Blog</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a href="/">Home</a>
                <a href="">Dashboard</a>
                <button id="logout">Logout</button>
                {{else}}
                <a href="/">Home</a>
                <a href="">Dashboard</a>
                <a href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
    </div>
</body>
</html>