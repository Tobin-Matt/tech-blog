{{#each posts as |post|}}
<div>
    <div>
        <h2>
            <a href="/post/{{post.id}}">{{post.title}}</a>
        </h2>
        <span>Posted By {{post.user.username}} on {{post.date_created}}</span>
    </div>
</div>
{{/each}}